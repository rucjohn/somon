# _*_ coding:utf-8 _*_

import os
from collect.utils import files

if __name__ == '_main__':
    basedir = os.path.dirname(os.path.realpath(__file__))
    app_version = utils.gen_code(files.getsize(basedir, suffix='py')

